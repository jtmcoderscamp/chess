function findKnightBaseMoves(){
    this.colour = color;
    this.boardPosition = position[x][y];
    this.possibleMoves = function(){
        //x+=2, y+=1
        //x+=2, y-=1
        //x-=2, y+=1
        //x-=2, y-=1
        //x+=1, y+=2
        //x+=1, y-=2
        //x+=1, y+=2
        //x+=1, y+=2
    }
}